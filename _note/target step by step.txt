7. 
6. workflow (spec, operation)
5. ERP Route

3. add BOM (list of Product)
-----------------------------------------
    add new column: alembic
    version -- not well, to check again later *************************************************
    ORM
2. add Product (CRUD)
modeling
1. make it run
    python -m venv venv
    source venv/bin/activate  # Windows: venv\Scripts\activate
    pip install -r requirements.txt
        # pip install "fastapi[all]" sqlmodel

    python main.py
    uvicorn main:app


pip install alembic
    alembic --version
alembic stamp base

alembic init alembic

alembic revision --autogenerate -m "initial migration"
    update file by add: import sqlmodel
alembic upgrade head
    alembic downgrade -1
